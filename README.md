# cas-mcp-connector
Standalone MCP server exposing CAS Engagement Manager tools to third-party AI clients (Claude Desktop, Gemini, etc.) with its own OAuth/CIAM login. EM-only; reuses CAS-MCP auth + audit middleware.
